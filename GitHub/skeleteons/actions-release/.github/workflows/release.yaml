---
# Template: 1.0.0
name: "Release"

on:
  push:
    tags:
      - "v?[0-9]+.[0-9]+.[0-9]+"
jobs:
  release:
    runs-on: "ubuntu-latest"
    steps:

      - name: "Check out repository code"
        uses: "actions/checkout@main"

      # "X_STEPS_PLACEHOLDER_X"
...
